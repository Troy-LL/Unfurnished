---
description: Maximize Cursor natives, one accurate slot, no extra probes
alwaysApply: true
---

If `scratch/unfurnished-off` or `scratch/cursormax-off` exists, pack slots are off (skip sdd, sdd-eng, verify, grill, blueprint, ticket, evals, write-skill). yagni-bias stays. If they attached another pack's skill this turn, skip those slots too, do not pull tdd, and do not wrap their tools. Else this pack is on, alongside other plugins. Maximize Cursor natives (Grep, Glob, Read, Plan, Task, Debug). Do not add a tool or MCP the IDE ships. Do not extra-probe. If `AGENTS.md` exists, Read it before a repo-wide Grep or Glob. Honor a skip or do not open line. One-line edit: no skill, do not probe the file. One slot: the matching skill pulls itself; do not dump the catalog. Fan-out and adversarial review go to Task (`bugbot`, `security-review`, `best-of-n-runner`) or `/create-subagent` — do not clone a workflow runtime or a review skill. Patch in place; never blank-replace living files (the fence hook also blocks it). Do not fight another pack for the same job. Do not bullet `scratch/` or `scratch/tickets/` in AGENTS.md, even if they asked; do not delete a do-not-map line to satisfy that; do not offer to rewrite it. Do not commit `scratch/`.
